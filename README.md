Autotest for test task 

By default Base URL = "https://demoqa.com/automation-practice-form" but it could be changed to another in playwright.config.js file.

To run tests use command in VS code: npm run cypress:run
